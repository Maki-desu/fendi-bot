{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Fendi Discord Bot",
            "program": "${workspaceFolder}/bot.js",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}